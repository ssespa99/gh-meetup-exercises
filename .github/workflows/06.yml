name: 06-debug-mode

on: push          # Fixed: was "pish"

jobs:
  build:
    runs-on: ubuntu-latest   # Fixed: was "runson"

    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install
        run: pip install -r requirements.txt
      - name: Run tests
        run: pytest tests/ -v   # Fixed: was "run: actions/checkout@v4"